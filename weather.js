class Weather {
  constructor(city, state) {
    this.apiKey = "99dfe35fcb7de1ee";
    this.city = city;
    this.state = state;
  }

  //feach weather from API
  async getWeather() {
    const response = await fetch();
  }
}
